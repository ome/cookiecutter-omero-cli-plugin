#!/usr/bin/env make -f
release:
ifndef VERSION
	$(error VERSION is undefined)
endif
	git describe --exact
	python setup.py sdist
	echo twine upload dist/{{cookicutter.directory_name}}-$(VERSION).tar.gz

clean:
	rm -rf build dist {{cookiecutter.directory_name}}.egg-info *.pyc

.PHONY: release clean
